# Sprinter — Swift-side verification gate.
#
# `make check` is the Swift analog of `bun run check` (policy.md §"SwiftUI /
# Swift side") and is exactly what CI runs on the macOS runner (FE1.3).

.PHONY: check
check:
	@bash scripts/check.sh
